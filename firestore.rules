rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERMISSIVE RULES FOR DEVELOPMENT (Allows anyone to read/write)
      // TODO: Secure this before production launch!
      allow read, write: if true;
    }
  }
}
